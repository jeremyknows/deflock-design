# CLAUDE.md — DeFlock Design

@AGENTS.md

CC-specific mirror: the full implementation handoff lives in `AGENTS.md` (imported above,
single source of truth — edit that file, not this one). Skill entry point: `SKILL.md`.
Start any design/UI task by following `SKILL.md`'s reading order; conflicts resolve up the
authority ladder in `AGENTS.md` §preamble.
