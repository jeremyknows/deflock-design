Footer strip — see SiteHeader.prompt.md for the paired chrome example.

```jsx
import { FooterStrip } from "./FooterStrip";
<FooterStrip contactEmail="contact@deflockonsol.example"/>
```

Carries, in order: exact disclaimer (`COMMUNITY-MADE · UNAFFILIATED WITH DEFLOCK.ORG`), quiet $DEFLOCK wordmark (Space Mono 700), "Not financial advice.", contact mailto. No other URLs (doctrine 3).
